#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../babili/bin/babili.js" "$@"
  ret=$?
else 
  node  "$basedir/../babili/bin/babili.js" "$@"
  ret=$?
fi
exit $ret
